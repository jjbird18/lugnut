<div class="form">
	<%= simple_form_for([@post, @post.comments.build]) do |f| %>
		<div class="form-inputs">
			<%= f.input :comment, placeholder: "Comment" %>
		</div>	
		<div class="form-actions">
			<%= f.button :submit, "Post Comment", class: 'cta' %>
		</div>	
	<% end %>
</div>