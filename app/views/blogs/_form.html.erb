<%= simple_form_for @blog, html: { multipart: true, class: 'directUpload', data: { 'form-data' => (@s3_direct_post.fields), 'url' => @s3_direct_post.url, 'host' => URI.parse(@s3_direct_post.url).host } } do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title, placeholder: "Title" %>
    <%= f.input :description, placeholder: "Description/Teaser" %>
    <%= f.input :main_content, placeholder: "Content" %>
    <%= f.label :image_file_name %><br />
    <%= f.file_field :image_file_name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Save", class: 'cta' %>
  </div>
<% end %>
